#include "Vehicle.h"

Vehicle::Vehicle(int id, int numberOfSeats, int MaxSpeed, double PriceForRent) : id(id), numberOfSeats(numberOfSeats), maxSpeed(MaxSpeed),priceForRent(PriceForRent){}

Vehicle::~Vehicle(){}
